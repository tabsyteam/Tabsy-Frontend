{
  "name": "@tabsy/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript type definitions for the Tabsy platform",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "^5.4.5"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
